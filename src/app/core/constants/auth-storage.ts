/**
 * Temporary client-side session keys.
 * Replace with a backend-managed strategy (e.g. httpOnly cookies) when NestJS auth is ready.
 */
export const ACCESS_TOKEN_KEY = 'access_token';
export const USER_KEY = 'current_user';
